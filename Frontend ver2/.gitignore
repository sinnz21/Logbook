# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-demo
dist-ssr
*.local

# Local environment — may hold API keys. Copy .env.example instead.
.env
.env.*
!.env.example
# Demo-mode flag — no secrets, must be committed so `npm run demo` works on a fresh clone.
!.env.demo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
